var todoList =	{
		todos: ['hello', 'there', 'bye']
	};

//Now  go to the console and call it.

todoList // Object {todos: Array(3)}
		 // todos: Array(3)
		 // __proto__: Object

todoList.todos // ["hello", "there", "bye"]

todoList.todos[0] //"hello"
todoList.todos[1] //"there"
todoList.todos[2] //"bye"